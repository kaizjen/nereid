/* Appearance.svelte generated by Svelte v3.48.0 */
import {
	SvelteComponentDev,
	add_flush_callback,
	add_location,
	append_dev,
	attr_dev,
	bind,
	binding_callbacks,
	component_subscribe,
	create_component,
	destroy_component,
	detach_dev,
	dispatch_dev,
	element,
	globals,
	init,
	insert_dev,
	is_function,
	mount_component,
	noop,
	safe_not_equal,
	set_store_value,
	space,
	text,
	transition_in,
	transition_out,
	validate_slots,
	validate_store
} from "nereid://.svelte/internal/index.mjs";

const { console: console_1 } = globals;
import { TextBlock, ComboBox, Checkbox, InfoBar, Slider } from "../modules/fluent-svelte/index.js";
import { getContext } from "nereid://.svelte/internal/index.mjs";
import noFirstTime from "nereid://js/nft.js";
const file = "Appearance.svelte";

// (67:2) <TextBlock>
function create_default_slot_3(ctx) {
	let t_1_value = /*t*/ ctx[4]('pages.settings.appearance.label-theme') + "";
	let t_1;

	const block = {
		c: function create() {
			t_1 = text(t_1_value);
		},
		m: function mount(target, anchor) {
			insert_dev(target, t_1, anchor);
		},
		p: noop,
		d: function destroy(detaching) {
			if (detaching) detach_dev(t_1);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_default_slot_3.name,
		type: "slot",
		source: "(67:2) <TextBlock>",
		ctx
	});

	return block;
}

// (79:2) <Checkbox      bind:checked={bookmarkBar}    >
function create_default_slot_2(ctx) {
	let t_1_value = /*t*/ ctx[4]('pages.settings.appearance.bookmarkBar') + "";
	let t_1;

	const block = {
		c: function create() {
			t_1 = text(t_1_value);
		},
		m: function mount(target, anchor) {
			insert_dev(target, t_1, anchor);
		},
		p: noop,
		d: function destroy(detaching) {
			if (detaching) detach_dev(t_1);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_default_slot_2.name,
		type: "slot",
		source: "(79:2) <Checkbox      bind:checked={bookmarkBar}    >",
		ctx
	});

	return block;
}

// (84:2) <TextBlock>
function create_default_slot_1(ctx) {
	let t_1_value = /*t*/ ctx[4]('pages.settings.appearance.pageZoom') + "";
	let t_1;

	const block = {
		c: function create() {
			t_1 = text(t_1_value);
		},
		m: function mount(target, anchor) {
			insert_dev(target, t_1, anchor);
		},
		p: noop,
		d: function destroy(detaching) {
			if (detaching) detach_dev(t_1);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_default_slot_1.name,
		type: "slot",
		source: "(84:2) <TextBlock>",
		ctx
	});

	return block;
}

// (92:2) <TextBlock>
function create_default_slot(ctx) {
	let t_1_value = /*t*/ ctx[4]('pages.settings.appearance.uiZoom') + "";
	let t_1;

	const block = {
		c: function create() {
			t_1 = text(t_1_value);
		},
		m: function mount(target, anchor) {
			insert_dev(target, t_1, anchor);
		},
		p: noop,
		d: function destroy(detaching) {
			if (detaching) detach_dev(t_1);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_default_slot.name,
		type: "slot",
		source: "(92:2) <TextBlock>",
		ctx
	});

	return block;
}

function create_fragment(ctx) {
	let div0;
	let textblock0;
	let t0;
	let combobox;
	let updating_value;
	let t1;
	let div1;
	let checkbox;
	let updating_checked;
	let t2;
	let div2;
	let textblock1;
	let t3;
	let slider0;
	let updating_value_1;
	let t4;
	let div3;
	let textblock2;
	let t5;
	let slider1;
	let updating_value_2;
	let current;

	textblock0 = new TextBlock({
			props: {
				$$slots: { default: [create_default_slot_3] },
				$$scope: { ctx }
			},
			$$inline: true
		});

	function combobox_value_binding(value) {
		/*combobox_value_binding*/ ctx[9](value);
	}

	let combobox_props = {
		items: [
			{
				name: /*t*/ ctx[4]('pages.settings.appearance.theme-dark'),
				value: 'dark'
			},
			{
				name: /*t*/ ctx[4]('pages.settings.appearance.theme-light'),
				value: 'light'
			},
			{
				name: /*t*/ ctx[4]('pages.settings.appearance.theme-system'),
				value: 'system'
			}
		]
	};

	if (/*theme*/ ctx[3].value !== void 0) {
		combobox_props.value = /*theme*/ ctx[3].value;
	}

	combobox = new ComboBox({ props: combobox_props, $$inline: true });
	binding_callbacks.push(() => bind(combobox, 'value', combobox_value_binding));

	combobox.$on("select", function () {
		if (is_function(/*theme*/ ctx[3].select)) /*theme*/ ctx[3].select.apply(this, arguments);
	});

	function checkbox_checked_binding(value) {
		/*checkbox_checked_binding*/ ctx[10](value);
	}

	let checkbox_props = {
		$$slots: { default: [create_default_slot_2] },
		$$scope: { ctx }
	};

	if (/*bookmarkBar*/ ctx[0] !== void 0) {
		checkbox_props.checked = /*bookmarkBar*/ ctx[0];
	}

	checkbox = new Checkbox({ props: checkbox_props, $$inline: true });
	binding_callbacks.push(() => bind(checkbox, 'checked', checkbox_checked_binding));

	textblock1 = new TextBlock({
			props: {
				$$slots: { default: [create_default_slot_1] },
				$$scope: { ctx }
			},
			$$inline: true
		});

	function slider0_value_binding(value) {
		/*slider0_value_binding*/ ctx[11](value);
	}

	let slider0_props = { suffix: "%", min: 1, max: 200 };

	if (/*defaultPageZoom_proxy*/ ctx[1] !== void 0) {
		slider0_props.value = /*defaultPageZoom_proxy*/ ctx[1];
	}

	slider0 = new Slider({ props: slider0_props, $$inline: true });
	binding_callbacks.push(() => bind(slider0, 'value', slider0_value_binding));

	textblock2 = new TextBlock({
			props: {
				$$slots: { default: [create_default_slot] },
				$$scope: { ctx }
			},
			$$inline: true
		});

	function slider1_value_binding(value) {
		/*slider1_value_binding*/ ctx[12](value);
	}

	let slider1_props = { suffix: "%", min: 1, max: 200 };

	if (/*chromeZoom_proxy*/ ctx[2] !== void 0) {
		slider1_props.value = /*chromeZoom_proxy*/ ctx[2];
	}

	slider1 = new Slider({ props: slider1_props, $$inline: true });
	binding_callbacks.push(() => bind(slider1, 'value', slider1_value_binding));

	const block = {
		c: function create() {
			div0 = element("div");
			create_component(textblock0.$$.fragment);
			t0 = space();
			create_component(combobox.$$.fragment);
			t1 = space();
			div1 = element("div");
			create_component(checkbox.$$.fragment);
			t2 = space();
			div2 = element("div");
			create_component(textblock1.$$.fragment);
			t3 = space();
			create_component(slider0.$$.fragment);
			t4 = space();
			div3 = element("div");
			create_component(textblock2.$$.fragment);
			t5 = space();
			create_component(slider1.$$.fragment);
			attr_dev(div0, "class", "s-option");
			add_location(div0, file, 65, 0, 1978);
			attr_dev(div1, "class", "s-option");
			add_location(div1, file, 77, 0, 2415);
			attr_dev(div2, "class", "s-option");
			add_location(div2, file, 82, 0, 2554);
			attr_dev(div3, "class", "s-option");
			add_location(div3, file, 90, 0, 2752);
		},
		l: function claim(nodes) {
			throw new Error("options.hydrate only works if the component was compiled with the `hydratable: true` option");
		},
		m: function mount(target, anchor) {
			insert_dev(target, div0, anchor);
			mount_component(textblock0, div0, null);
			append_dev(div0, t0);
			mount_component(combobox, div0, null);
			insert_dev(target, t1, anchor);
			insert_dev(target, div1, anchor);
			mount_component(checkbox, div1, null);
			insert_dev(target, t2, anchor);
			insert_dev(target, div2, anchor);
			mount_component(textblock1, div2, null);
			append_dev(div2, t3);
			mount_component(slider0, div2, null);
			insert_dev(target, t4, anchor);
			insert_dev(target, div3, anchor);
			mount_component(textblock2, div3, null);
			append_dev(div3, t5);
			mount_component(slider1, div3, null);
			current = true;
		},
		p: function update(new_ctx, [dirty]) {
			ctx = new_ctx;
			const textblock0_changes = {};

			if (dirty & /*$$scope*/ 524288) {
				textblock0_changes.$$scope = { dirty, ctx };
			}

			textblock0.$set(textblock0_changes);
			const combobox_changes = {};

			if (!updating_value && dirty & /*theme*/ 8) {
				updating_value = true;
				combobox_changes.value = /*theme*/ ctx[3].value;
				add_flush_callback(() => updating_value = false);
			}

			combobox.$set(combobox_changes);
			const checkbox_changes = {};

			if (dirty & /*$$scope*/ 524288) {
				checkbox_changes.$$scope = { dirty, ctx };
			}

			if (!updating_checked && dirty & /*bookmarkBar*/ 1) {
				updating_checked = true;
				checkbox_changes.checked = /*bookmarkBar*/ ctx[0];
				add_flush_callback(() => updating_checked = false);
			}

			checkbox.$set(checkbox_changes);
			const textblock1_changes = {};

			if (dirty & /*$$scope*/ 524288) {
				textblock1_changes.$$scope = { dirty, ctx };
			}

			textblock1.$set(textblock1_changes);
			const slider0_changes = {};

			if (!updating_value_1 && dirty & /*defaultPageZoom_proxy*/ 2) {
				updating_value_1 = true;
				slider0_changes.value = /*defaultPageZoom_proxy*/ ctx[1];
				add_flush_callback(() => updating_value_1 = false);
			}

			slider0.$set(slider0_changes);
			const textblock2_changes = {};

			if (dirty & /*$$scope*/ 524288) {
				textblock2_changes.$$scope = { dirty, ctx };
			}

			textblock2.$set(textblock2_changes);
			const slider1_changes = {};

			if (!updating_value_2 && dirty & /*chromeZoom_proxy*/ 4) {
				updating_value_2 = true;
				slider1_changes.value = /*chromeZoom_proxy*/ ctx[2];
				add_flush_callback(() => updating_value_2 = false);
			}

			slider1.$set(slider1_changes);
		},
		i: function intro(local) {
			if (current) return;
			transition_in(textblock0.$$.fragment, local);
			transition_in(combobox.$$.fragment, local);
			transition_in(checkbox.$$.fragment, local);
			transition_in(textblock1.$$.fragment, local);
			transition_in(slider0.$$.fragment, local);
			transition_in(textblock2.$$.fragment, local);
			transition_in(slider1.$$.fragment, local);
			current = true;
		},
		o: function outro(local) {
			transition_out(textblock0.$$.fragment, local);
			transition_out(combobox.$$.fragment, local);
			transition_out(checkbox.$$.fragment, local);
			transition_out(textblock1.$$.fragment, local);
			transition_out(slider0.$$.fragment, local);
			transition_out(textblock2.$$.fragment, local);
			transition_out(slider1.$$.fragment, local);
			current = false;
		},
		d: function destroy(detaching) {
			if (detaching) detach_dev(div0);
			destroy_component(textblock0);
			destroy_component(combobox);
			if (detaching) detach_dev(t1);
			if (detaching) detach_dev(div1);
			destroy_component(checkbox);
			if (detaching) detach_dev(t2);
			if (detaching) detach_dev(div2);
			destroy_component(textblock1);
			destroy_component(slider0);
			if (detaching) detach_dev(t4);
			if (detaching) detach_dev(div3);
			destroy_component(textblock2);
			destroy_component(slider1);
		}
	};

	dispatch_dev("SvelteRegisterBlock", {
		block,
		id: create_fragment.name,
		type: "component",
		source: "",
		ctx
	});

	return block;
}

function throttle(func, ms = 200) {
	let timeout;

	return function wrapper() {
		if (timeout) {
			clearTimeout(timeout);
		}

		timeout = setTimeout(func, ms);
	};
}

function instance($$self, $$props, $$invalidate) {
	let $config;
	let { $$slots: slots = {}, $$scope } = $$props;
	validate_slots('Appearance', slots, []);
	let { update } = $$props;
	const { t } = window.nereid.i18n;
	let config = getContext('config');
	validate_store(config, 'config');
	component_subscribe($$self, config, value => $$invalidate(13, $config = value));

	const theme = {
		select({ detail }) {
			console.log('selected', detail);
			set_store_value(config, $config.ui.theme = detail.value, $config);
			update();
		},
		value: $config.ui.theme
	};

	let bookmarkBar = $config.ui.showBookmarks;

	const updateBookmarkBar = noFirstTime(() => {
		console.log('bm-b', bookmarkBar);
		set_store_value(config, $config.ui.showBookmarks = bookmarkBar, $config);
		update();
	});

	let defaultPageZoom = $config.ui.defaultZoomFactor * 100;
	let defaultPageZoom_proxy = defaultPageZoom; // proxy to not trigger write operation EVERY time the slider is changed

	const updatePageZoom = noFirstTime(() => {
		console.log('z-page', defaultPageZoom);
		set_store_value(config, $config.ui.defaultZoomFactor = Number((defaultPageZoom / 100).toPrecision(2)), $config);
		update();
	});

	const pageZoonereidrottled = throttle(() => {
		$$invalidate(7, defaultPageZoom = defaultPageZoom_proxy);
	});

	let chromeZoom = $config.ui.chromeZoomFactor * 100;
	let chromeZoom_proxy = chromeZoom;

	const updateChromeZoom = noFirstTime(() => {
		console.log('z-chr', chromeZoom);
		set_store_value(config, $config.ui.chromeZoomFactor = Number((chromeZoom / 100).toPrecision(2)), $config);
		update();
	});

	const chromeZoonereidrottled = throttle(() => {
		$$invalidate(8, chromeZoom = chromeZoom_proxy);
	});

	const writable_props = ['update'];

	Object.keys($$props).forEach(key => {
		if (!~writable_props.indexOf(key) && key.slice(0, 2) !== '$$' && key !== 'slot') console_1.warn(`<Appearance> was created with unknown prop '${key}'`);
	});

	function combobox_value_binding(value) {
		if ($$self.$$.not_equal(theme.value, value)) {
			theme.value = value;
			$$invalidate(3, theme);
		}
	}

	function checkbox_checked_binding(value) {
		bookmarkBar = value;
		$$invalidate(0, bookmarkBar);
	}

	function slider0_value_binding(value) {
		defaultPageZoom_proxy = value;
		$$invalidate(1, defaultPageZoom_proxy);
	}

	function slider1_value_binding(value) {
		chromeZoom_proxy = value;
		$$invalidate(2, chromeZoom_proxy);
	}

	$$self.$$set = $$props => {
		if ('update' in $$props) $$invalidate(6, update = $$props.update);
	};

	$$self.$capture_state = () => ({
		TextBlock,
		ComboBox,
		Checkbox,
		InfoBar,
		Slider,
		getContext,
		noFirstTime,
		update,
		t,
		throttle,
		config,
		theme,
		bookmarkBar,
		updateBookmarkBar,
		defaultPageZoom,
		defaultPageZoom_proxy,
		updatePageZoom,
		pageZoonereidrottled,
		chromeZoom,
		chromeZoom_proxy,
		updateChromeZoom,
		chromeZoonereidrottled,
		$config
	});

	$$self.$inject_state = $$props => {
		if ('update' in $$props) $$invalidate(6, update = $$props.update);
		if ('config' in $$props) $$invalidate(5, config = $$props.config);
		if ('bookmarkBar' in $$props) $$invalidate(0, bookmarkBar = $$props.bookmarkBar);
		if ('defaultPageZoom' in $$props) $$invalidate(7, defaultPageZoom = $$props.defaultPageZoom);
		if ('defaultPageZoom_proxy' in $$props) $$invalidate(1, defaultPageZoom_proxy = $$props.defaultPageZoom_proxy);
		if ('chromeZoom' in $$props) $$invalidate(8, chromeZoom = $$props.chromeZoom);
		if ('chromeZoom_proxy' in $$props) $$invalidate(2, chromeZoom_proxy = $$props.chromeZoom_proxy);
	};

	if ($$props && "$$inject" in $$props) {
		$$self.$inject_state($$props.$$inject);
	}

	$$self.$$.update = () => {
		if ($$self.$$.dirty & /*bookmarkBar*/ 1) {
			$: {
				bookmarkBar;
				updateBookmarkBar();
			}
		}

		if ($$self.$$.dirty & /*defaultPageZoom*/ 128) {
			$: {
				defaultPageZoom;
				updatePageZoom();
			}
		}

		if ($$self.$$.dirty & /*defaultPageZoom_proxy*/ 2) {
			$: {
				defaultPageZoom_proxy;
				pageZoonereidrottled();
			}
		}

		if ($$self.$$.dirty & /*chromeZoom*/ 256) {
			$: {
				chromeZoom;
				updateChromeZoom();
			}
		}

		if ($$self.$$.dirty & /*chromeZoom_proxy*/ 4) {
			$: {
				chromeZoom_proxy;
				chromeZoonereidrottled();
			}
		}
	};

	return [
		bookmarkBar,
		defaultPageZoom_proxy,
		chromeZoom_proxy,
		theme,
		t,
		config,
		update,
		defaultPageZoom,
		chromeZoom,
		combobox_value_binding,
		checkbox_checked_binding,
		slider0_value_binding,
		slider1_value_binding
	];
}

class Appearance extends SvelteComponentDev {
	constructor(options) {
		super(options);
		init(this, options, instance, create_fragment, safe_not_equal, { update: 6 });

		dispatch_dev("SvelteRegisterComponent", {
			component: this,
			tagName: "Appearance",
			options,
			id: create_fragment.name
		});

		const { ctx } = this.$$;
		const props = options.props || {};

		if (/*update*/ ctx[6] === undefined && !('update' in props)) {
			console_1.warn("<Appearance> was created without expected prop 'update'");
		}
	}

	get update() {
		throw new Error("<Appearance>: Props cannot be read directly from the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}

	set update(value) {
		throw new Error("<Appearance>: Props cannot be set directly on the component instance unless compiling with 'accessors: true' or '<svelte:options accessors/>'");
	}
}

export default Appearance;